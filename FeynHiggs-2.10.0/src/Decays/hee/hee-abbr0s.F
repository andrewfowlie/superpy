#if 0
* hee-abbr0s.F
* abbreviations for {S[10, {hno}]} -> {F[2, {gno1}], -F[2, {gno2}]}
* generated by FormCalc 8.3 on 3-Dec-2013 16:54
* this file is part of FeynHiggs
#endif

#include "hee-vars.h"
#include "inline.h"

	subroutine hee_abbr0s
	implicit none

#include "hee-vars.h"

#include "inline.h"

	Sub1 = SA*XHiggs(hno,1,uzext) - CA*XHiggs(hno,2,uzext)

	Sub2 = Sub1 + cI*(SB*XHiggs(hno,3,uzext))

	Sub3 = cI*Sub1 + SB*XHiggs(hno,3,uzext)

	end
