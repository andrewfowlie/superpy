#if 0
* huu-abbr0s.F
* abbreviations for {S[10, {hno}]} -> {F[3, {gno1, Col2}], -F[3, {gno2, Col3}]}
* generated by FormCalc 8.3 on 3-Dec-2013 16:57
* this file is part of FeynHiggs
#endif

#include "huu-vars.h"
#include "inline.h"

	subroutine huu_abbr0s
	implicit none

#include "huu-vars.h"

#include "inline.h"

	Sub1 = CA*XHiggs(hno,1,uzext) + SA*XHiggs(hno,2,uzext)

	Sub2 = cI*(Sub1*TB) + SB*XHiggs(hno,3,uzext)

	Sub3 = -((cI*(Sub1*TB) - SB*XHiggs(hno,3,uzext))/(SB*TB))

	MatSUN(1,1) = 3

	end
