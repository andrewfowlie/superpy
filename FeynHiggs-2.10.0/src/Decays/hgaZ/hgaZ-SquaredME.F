*#define CHECK

#if 0
* hgaZ-SquaredME.F
* assembly of squared matrix element
* generated by FormCalc 8.3 on 3-Dec-2013 16:50
* this file is part of FeynHiggs
#endif

#include "hgaZ-vars.h"
#include "inline.h"

************************************************************************

	subroutine hgaZ_SquaredMEHel(res)
	implicit none
	RealType res(*)

#include "hgaZ-vars.h"

	ComplexType amp
	RealType ampsq

* hgaZ-BEGIN ABBR_HEL
	call hgaZ_abbr1h
* hgaZ-END ABBR_HEL

	ampsq = 0
	res(1) = ampsq

	ampsq = 0
* hgaZ-BEGIN FF_LOOP
	Cloop(1) = 0

	call hgaZ_v1

	LOOP(Gen4, 1,3,1)
	call hgaZ_v1_Gen4
	ENDLOOP(Gen4)

	LOOP(Cha5, 1,2,1)
	LOOP(Cha4, 1,2,1)
	call hgaZ_v1_Cha4_Cha5
	ENDLOOP(Cha4)
	ENDLOOP(Cha5)

	LOOP(Ind3, 1,3,1)
	LOOP(Ind2, 1,3,1)
	LOOP(Ind1, 1,3,1)
	LOOP(All5, 1,6,1)
	LOOP(All4, 1,6,1)
	call hgaZ_v1_All4_All5_Ind1Ind2Ind3
	ENDLOOP(All4)
	ENDLOOP(All5)
	ENDLOOP(Ind1)
	ENDLOOP(Ind2)
	ENDLOOP(Ind3)
* hgaZ-END FF_LOOP

* hgaZ-BEGIN M2_LOOP
	amp = 0
	amp = amp + 1*Cloop(1)
	ampsq = ampsq + Re(Conjugate(Cloop(1))*amp)
* hgaZ-END M2_LOOP
	res(2) = ampsq
	end

************************************************************************

	subroutine hgaZ_SquaredME(result)
	implicit none
	RealType result(*)

#include "hgaZ-vars.h"

* hgaZ-BEGIN VAR_DECL
	integer i, h, hmax, hsimd
	parameter (hmax = 2)
	parameter (hsimd = hmax)
	RealType res(2,hsimd)
	RealType rtree, rloop
	external hgaZ_SquaredMEHel
* hgaZ-END VAR_DECL

#include "inline.h"

* hgaZ-BEGIN INVARIANTS
* hgaZ-END INVARIANTS

* hgaZ-BEGIN ABBR_S
	call hgaZ_abbr1s
* hgaZ-END ABBR_S

* hgaZ-BEGIN ABBR_ANGLE
* hgaZ-END ABBR_ANGLE

* hgaZ-BEGIN HEL_LOOP
	h = 0

	do i = 0, hmax - 1

	  Hel(1) = 0
	  Hel(2) = -1 + 2*Mod(i/1,2)
	  Hel(3) = Hel(2)

	  h = h + 1; call hgaZ_SquaredMEHel(res(1,h))
	enddo

* hgaZ-END HEL_LOOP

* hgaZ-BEGIN RESULT
	rtree = 0
	rloop = 0
	do i = 1, h
	  rtree = rtree + res(1,i)
	  rloop = rloop + res(2,i)
	enddo
	result(1) = rtree
#ifdef WF_RENORMALIZATION
	rloop = rloop + (WF_RENORMALIZATION)*rtree
#endif
	result(2) = rloop
* hgaZ-END RESULT

#ifdef CHECK
	print *, 'tree =', rtree
	print *, 'loop =', rloop
	stop
#endif

* hgaZ-END SQUAREDME
	end

