#if 0
* hhh-abbr0s.F
* abbreviations for {S[10, {hno}]} -> {S[10, {hno1}], S[10, {hno2}]}
* generated by FormCalc 8.3 on 3-Dec-2013 16:52
* this file is part of FeynHiggs
#endif

#include "hhh-vars.h"
#include "inline.h"

	subroutine hhh_abbr0s
	implicit none

#include "hhh-vars.h"

#include "inline.h"

        Sub2 = XHiggs(hno1,2,uzext)*XHiggs(hno2,1,uzext) + 
     &    XHiggs(hno1,1,uzext)*XHiggs(hno2,2,uzext)

        Sub3 = Sub2*XHiggs(hno,1,uzext) + 
     &    XHiggs(hno,2,uzext)*XHiggs(hno1,1,uzext)*
     &     XHiggs(hno2,1,uzext)

        Sub4 = Sub2*XHiggs(hno,2,uzext) + 
     &    XHiggs(hno,1,uzext)*XHiggs(hno1,2,uzext)*
     &     XHiggs(hno2,2,uzext)

        Sub6 = XHiggs(hno1,3,uzext)*XHiggs(hno2,1,uzext) + 
     &    XHiggs(hno1,1,uzext)*XHiggs(hno2,3,uzext)

        Sub8 = XHiggs(hno1,3,uzext)*XHiggs(hno2,2,uzext) + 
     &    XHiggs(hno1,2,uzext)*XHiggs(hno2,3,uzext)

        Sub1 = SAB*XHiggs(hno,1,uzext)*XHiggs(hno1,1,uzext)*
     &     XHiggs(hno2,1,uzext) + 
     &    CAB*XHiggs(hno,2,uzext)*XHiggs(hno1,2,uzext)*
     &     XHiggs(hno2,2,uzext)

        Sub7 = Sub6*XHiggs(hno,3,uzext) + 
     &    XHiggs(hno,1,uzext)*XHiggs(hno1,3,uzext)*
     &     XHiggs(hno2,3,uzext)

        Sub9 = Sub8*XHiggs(hno,3,uzext) + 
     &    XHiggs(hno,2,uzext)*XHiggs(hno1,3,uzext)*
     &     XHiggs(hno2,3,uzext)

        Sub5 = -(S2A*(2*SAB*Sub3 - 2*CAB*Sub4)) + 
     &    C2A*(CAB*Sub3 + SAB*Sub4)

	end
